<% if parent_type == :question %>
  $('.question_container .comments').html('<%= j render 'comments_list' %>');
<% elsif parent_type == :answer %>
  $('#answer-<%= parent.id %> .answer-comments').html('<%= j render 'comments_list', parent: parent %>');
<% end %> 